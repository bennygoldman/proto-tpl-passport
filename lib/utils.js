// simpleSort(key, direction)
// declares a = a.key
// declares b = b.key
// checks type of key value
// // picks sort pattern based on type
// direction must be ascending or descending
// // if ascending, a-b; else b-a 
// sort(a, b) => key(A || B).sortPattern.key(B || A)

// simplePlurals(key, count = 0)
// pull this from relisten

// fetchData

/**
const snippets = {
    sort_text: ` const byWard = setLibrary.sort((a, b) => {
        return a.ward.toLowerCase().localeCompare(b.ward.toLowerCase());

    });
    console.log(byWard);`,

    fetch_helper: `function fetchLibraryData() {
        return fetch('response.json')
       .then(response => response.json());
       }`
};
 */